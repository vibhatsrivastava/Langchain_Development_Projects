# ─── Testing / Dev tooling ────────────────────────────────────
# Runtime dependencies (langchain, langgraph, langchain-ollama, langchain-community,
# python-dotenv, hvac, etc.) are declared in common/pyproject.toml and installed via:
#   uv pip install -e ./common
pytest>=8.0.0             # Modern Python testing framework
pytest-cov>=4.1.0         # Coverage plugin for pytest
pytest-mock>=3.12.0       # Mocking utilities for pytest
pytest-asyncio>=0.23.0    # Async test support for pytest
responses>=0.24.0         # Mock HTTP requests for testing
requests>=2.31.0          # Common HTTP client used by multiple projects
