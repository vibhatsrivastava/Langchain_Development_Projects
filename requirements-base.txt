# ─── Core LangChain ──────────────────────────────────────────
langchain
langchain-community
langchain-ollama          # Dedicated Ollama integration (preferred over community)
langgraph                 # For stateful multi-step agentic workflows

# ─── Utilities ───────────────────────────────────────────────
python-dotenv             # Load .env files
hvac>=2.0.0               # HashiCorp Vault client for centralized secret management
httpx                     # HTTP client used by langchain-ollama for auth headers

# ─── Testing ─────────────────────────────────────────────────
pytest>=8.0.0             # Modern Python testing framework
pytest-cov>=4.1.0         # Coverage plugin for pytest
pytest-mock>=3.12.0       # Mocking utilities for pytest
pytest-asyncio>=0.23.0    # Async test support for pytest
responses>=0.24.0         # Mock HTTP requests for testing
